// Optional: carrier API keys for live tracking status.
// Without these the app shows a link to the carrier's tracking page instead.
export const CONFIG = {
  postnord: { apiKey: '' },
  dhl: { apiKey: '' },
};
